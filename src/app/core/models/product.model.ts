export interface Product {
    id?: number; // id è opzionale quando crei un nuovo prodotto
    name: string;
    description: string;
    price: number;
    imageUrl: string;
    category: string;
  }
  